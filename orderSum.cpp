#include <iostream>
using namespace std;

int main()
{
    string eq, temp;

    cin >> eq;
    int length = eq.length();

    for (int i = 0; i < length; i + 2)
    {
        if (eq[i] < eq[i + 2])
        {
            temp =
        }
    }
}